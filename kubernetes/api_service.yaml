apiVersion: v1
kind: Service
metadata:
  labels:
    run: libraryproducer
  name: libraryproducer
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    run: libraryproducer-exposed
  type: NodePort
